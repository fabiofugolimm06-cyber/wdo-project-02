[pytest]
testpaths = tests
pythonpath = .
markers =
    fast: testes core do pipeline (default CI)
    slow: testes de validacao pesada (certification, audit, snapshot)
    long: testes de stress e execucao prolongada (determinism, recovery, rollback)
    ci_determinism: stress de determinismo para pipeline CI (10 iteracoes)
